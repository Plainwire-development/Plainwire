{
  "name": "plainwire-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:scss && npm run build:elm",
    "build:scss": "sass priv/static/style.scss priv/static/app.css --no-source-map",
    "build:elm": "bash scripts/build-elm.sh",
    "dev": "npm run build:scss && bash scripts/build-elm.sh"
  },
  "devDependencies": {
    "sass": "^1.101.0"
  }
}
