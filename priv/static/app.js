(()=>{'use strict';
const $=(q,r=document)=>r.querySelector(q), $$=(q,r=document)=>[...r.querySelectorAll(q)];
const app=$('#app');
const S={me:null,csrf:'',forums:[],servers:[],convs:[],friends:[],notifs:[],route:'',active:{kind:'home'},drafts:JSON.parse(localStorage.pw_drafts||'{}'),msg:[],ws:null,subs:new Set(),lastSync:0,voice:{mode:null,id:null,stream:null,peers:new Map(),users:new Map(),muted:false,deafened:false}};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const ago=t=>{if(!t)return'never';let s=Math.max(1,Math.floor((Date.now()-t)/1000));if(s<60)return s+'s';let m=Math.floor(s/60);if(m<60)return m+'m';let h=Math.floor(m/60);if(h<24)return h+'h';return Math.floor(h/24)+'d'};
const fmt=t=>new Date(t).toLocaleString([], {month:'short',day:'numeric',hour:'2-digit',minute:'2-digit'});
const saveDrafts=()=>localStorage.pw_drafts=JSON.stringify(S.drafts);
async function api(method,path,body){let opt={method,headers:{'accept':'application/json'}};if(body!==undefined){opt.headers['content-type']='application/json';opt.headers['x-csrf-token']=S.csrf;opt.body=JSON.stringify(body)}let r=await fetch('/api'+path,opt);let j=await r.json().catch(()=>({ok:false,error:'bad_json'}));if(!j.ok)throw Object.assign(new Error(j.error||'request_failed'),{status:r.status});return j.data}
function go(h){location.hash=h}
function avatar(u,cls=''){let src=u&&u.avatar_url;if(src)return`<img class="avatar ${cls}" src="${esc(src)}" alt="">`;let n=(u?.display_name||u?.username||'?').slice(0,1).toUpperCase();return`<div class="avatar ${cls}">${esc(n)}</div>`}
function iconServer(s){if(s?.icon_url)return`<img class="server-icon" src="${esc(s.icon_url)}" alt="">`;return`<div class="server-icon">${esc((s?.name||'?').slice(0,1).toUpperCase())}</div>`}
function toast(t){let n=document.createElement('div');n.className='toast';n.textContent=t;document.body.appendChild(n);setTimeout(()=>n.remove(),2600)}
function shell(main,side='',right=''){return `<div class="layout"><nav class="rail"><div class="mark" title="Plainwire"></div><button class="rail-btn ${S.active.kind==='home'?'active':''}" data-go="#">⌂</button><button class="rail-btn ${S.active.kind==='forum'?'active':''}" data-go="#forums">F</button><button class="rail-btn ${S.active.kind==='dm'?'active':''}" data-go="#dms">D</button><button class="rail-btn ${S.active.kind==='friends'?'active':''}" data-go="#friends">+ </button><div class="rail-spacer"></div><button class="rail-btn" data-go="#settings">⚙</button></nav>${side||sideDefault()}<main class="main">${main}</main>${right||rightDefault()}</div>`}
function sideDefault(){let unread=S.notifs.filter(n=>!n.seen).length;return `<aside class="side"><div class="side-head"><h1>Plainwire</h1><small>Relay v1</small><div class="nav-actions"><button class="btn secondary" data-go="#new-server">New server</button><button class="btn secondary" data-action="joinInvite">Join</button></div></div><div class="search"><input id="globalSearch" placeholder="Search users and threads"></div><div class="list"><a class="row" data-go="#notifications"><span class="badge">${unread}</span><div class="grow"><b>Notifications</b><small class="muted">live updates</small></div></a><a class="row" data-go="#friends"><span class="server-icon">+</span><div class="grow"><b>Friends</b><small class="muted">requests and contacts</small></div></a>${S.servers.map(s=>`<a class="row" data-go="#server/${s.id}">${iconServer(s)}<div class="grow"><b>${esc(s.name)}</b><small class="muted">${esc(s.role)} · ${s.member_count} members</small></div></a>`).join('')}<div class="row"><div class="grow"><b>Direct Messages</b><small class="muted">private and group chats</small></div><button class="btn secondary" data-action="newDM">New</button></div>${S.convs.map(c=>`<a class="row" data-go="#dm/${c.id}"><span class="badge">${c.unread||0}</span><div class="grow"><b>${esc(convName(c))}</b><small class="muted">${esc(c.last_body||'No messages yet')}</small></div></a>`).join('')}</div></aside>`}
function rightDefault(){return `<aside class="right"><h3>Profile</h3><div class="pad">${avatar(S.me,'big')}<h3>${esc(S.me?.display_name||'')}</h3><p class="muted">@${esc(S.me?.username||'')}</p><p>${esc(S.me?.bio||'No bio set.')}</p><span class="pill">${esc(S.me?.status||'online')}</span></div><div id="voiceBox" class="voice"></div></aside>`}
function convName(c){return c.name||`Group DM ${c.id}`}
function wire(){ $$('[data-go]').forEach(e=>e.onclick=ev=>{ev.preventDefault();go(e.dataset.go)}); $$('[data-action="joinInvite"]').forEach(e=>e.onclick=joinInvite); $$('[data-action="newDM"]').forEach(e=>e.onclick=newDMModal); let gs=$('#globalSearch'); if(gs)gs.onkeydown=e=>{if(e.key==='Enter'&&gs.value.trim())go('#search/'+encodeURIComponent(gs.value.trim()))}; renderVoiceBox();}
async function boot(){try{let d=await api('GET','/me'); S.me=d.user; S.csrf=d.csrf; S.lastSync=d.server_time||Date.now(); connectWS(); await silentSync(true); render(); setInterval(()=>silentSync(false),3500)}catch(e){renderAuth()}}
function renderAuth(){app.innerHTML=`<div class="layout"><div></div><main class="main"><div class="content"><div class="card pad" style="max-width:520px;margin:8vh auto"><h1>Plainwire Relay</h1><p class="muted">Independent forum, chat, server, and voice relay.</p><div class="tabs"><button class="btn" id="loginTab">Login</button><button class="btn secondary" id="regTab">Register</button></div><div id="authForm"></div></div></div></main></div>`;let mode='login';let draw=()=>{$('#authForm').innerHTML=`<div class="field"><label>Username</label><input id="u" autocomplete="username"></div>${mode==='register'?'<div class="field"><label>Display name</label><input id="d"></div>':''}<div class="field"><label>Password</label><input id="p" type="password" autocomplete="current-password"></div><button class="btn" id="authBtn">${mode==='login'?'Login':'Create account'}</button>`;$('#authBtn').onclick=async()=>{try{let path=mode==='login'?'/login':'/register';let data=await api('POST',path,{username:$('#u').value,display_name:$('#d')?.value||$('#u').value,password:$('#p').value});S.me=data.user;S.csrf=data.csrf;connectWS();await silentSync(true);render()}catch(e){toast(e.message)}}};$('#loginTab').onclick=()=>{mode='login';draw()};$('#regTab').onclick=()=>{mode='register';draw()};draw()}
async function silentSync(force){try{let d=await api('GET','/sync?since='+S.lastSync);S.lastSync=d.now;S.notifs=d.notifications||[];S.convs=d.conversations||[];S.servers=d.servers||[];S.friends=d.friends||[];if(force||!S.me){let me=await api('GET','/me');S.me=me.user;S.csrf=me.csrf}updateShellBits();await updateActiveMessages()}catch(e){if(e.status===401)renderAuth()}}
function updateShellBits(){let unread=S.notifs.filter(n=>!n.seen).length;document.title=unread?`(${unread}) Plainwire Relay`:'Plainwire Relay';let side=document.querySelector('.side');if(side&&!document.activeElement?.closest('.composer')){/* keep lists fresh but don't touch main */}}
function connectWS(){if(S.ws)try{S.ws.close()}catch{};let proto=location.protocol==='https:'?'wss':'ws';let ws=new WebSocket(proto+'://'+location.host+'/ws');S.ws=ws;ws.onopen=()=>resub();ws.onmessage=e=>handleEvent(JSON.parse(e.data));ws.onclose=()=>setTimeout(connectWS,1800)}
function sendWS(o){if(S.ws&&S.ws.readyState===1)S.ws.send(JSON.stringify(o))}
function subscribe(k){S.subs.add(k);sendWS({type:'subscribe',key:k})}
function clearSubs(){S.subs.clear();sendWS({type:'unsubscribe_all'})}
function resub(){for(let k of S.subs)sendWS({type:'subscribe',key:k})}
async function handleEvent(ev){if(ev.type==='notification'){await silentSync(false);toast('New notification');return}if(ev.type==='message_created'){if((S.active.kind==='channel'&&ev.scope==='channel'&&ev.scope_id===S.active.id)||(S.active.kind==='dm'&&ev.scope==='direct'&&ev.scope_id===S.active.id))appendMessage(ev.message);else silentSync(false);return}if(ev.type==='thread_reply'&&S.active.kind==='thread'&&ev.thread_id===S.active.id){appendReply(ev.reply);return}if(ev.type&&ev.type.startsWith('voice'))return voiceEvent(ev);if(ev.type&&ev.type.startsWith('call'))return callEvent(ev);silentSync(false)}
function render(){let h=location.hash.slice(1)||'home';S.route=h;clearSubs();if(h==='home')return renderHome();if(h==='forums')return renderForums();if(h.startsWith('forum/'))return renderForum(+h.split('/')[1]);if(h.startsWith('thread/'))return renderThread(+h.split('/')[1]);if(h==='dms')return renderDMs();if(h.startsWith('dm/'))return renderDM(+h.split('/')[1]);if(h==='friends')return renderFriends();if(h.startsWith('profile/'))return renderProfile(+h.split('/')[1]);if(h==='settings')return renderSettings();if(h==='new-server')return renderNewServer();if(h.startsWith('server/'))return renderServer(+h.split('/')[1]);if(h.startsWith('channel/'))return renderChannel(+h.split('/')[1]);if(h.startsWith('voice/'))return renderVoiceChannel(+h.split('/')[1]);if(h.startsWith('invite/'))return acceptInvite(h.split('/')[1]);if(h==='notifications')return renderNotifications();if(h.startsWith('search/'))return renderSearch(decodeURIComponent(h.slice(7)));renderHome()}
function renderHome(){S.active={kind:'home'};app.innerHTML=shell(`<div class="topbar"><h2>Home</h2></div><div class="content"><div class="grid"><div class="card pad"><h2>Relay</h2><p>A persistent chat/forum workspace with servers, groups, direct messages, invites, profiles, and peer-to-peer voice signaling.</p></div><div class="card pad"><h2>Unread</h2><p class="muted">${S.notifs.filter(n=>!n.seen).length} notifications · ${S.convs.reduce((a,c)=>a+(c.unread||0),0)} direct unread</p></div></div></div>`);wire()}
async function renderForums(){S.active={kind:'forum'};S.forums=await api('GET','/forums');app.innerHTML=shell(`<div class="topbar"><h2>Forums</h2><button class="btn" id="newThread">New thread</button></div><div class="content"><div class="grid">${S.forums.map(f=>`<div class="card pad" data-go="#forum/${f.id}"><h2>${esc(f.name)}</h2><p class="muted">${esc(f.description)}</p><span class="pill">${f.thread_count} threads</span> <span class="pill">${f.reply_count} replies</span></div>`).join('')}</div></div>`);wire();$('#newThread').onclick=newThreadModal}
async function renderForum(id){S.active={kind:'forum',id};subscribe('forum:'+id);let threads=await api('GET','/threads?forum_id='+id);app.innerHTML=shell(`<div class="topbar"><h2>Forum</h2><button class="btn" id="newThread">New thread</button></div><div class="content"><div class="card">${threads.map(t=>`<a class="row" data-go="#thread/${t.id}"><div class="grow"><b>${esc(t.title)}</b><small class="muted">${esc(t.display_name)} · ${t.reply_count} replies · ${t.views} views · ${ago(t.updated_at)} ago</small></div></a>`).join('')||'<div class="empty">No threads yet.</div>'}</div></div>`);wire();$('#newThread').onclick=()=>newThreadModal(id)}
function newThreadModal(fid){modal(`<h2>New thread</h2><div class="field"><label>Forum</label><select id="forumPick">${S.forums.map(f=>`<option value="${f.id}" ${f.id===fid?'selected':''}>${esc(f.name)}</option>`).join('')}</select></div><div class="field"><label>Title</label><input id="tTitle"></div><div class="field"><label>Body</label><textarea id="tBody" rows="8"></textarea></div><button class="btn" id="createThread">Create</button>`);$('#createThread').onclick=async()=>{let r=await api('POST','/threads',{forum_id:+$('#forumPick').value,title:$('#tTitle').value,body:$('#tBody').value});closeModal();go('#thread/'+r.id)}}
async function renderThread(id){S.active={kind:'thread',id};subscribe('thread:'+id);let d=await api('GET','/thread/'+id);let t=d.thread;app.innerHTML=shell(`<div class="topbar"><h2>${esc(t.forum_name)}</h2></div><div class="content" id="threadContent"><div class="post card"><h1 class="thread-title">${esc(t.title)}</h1><div class="post-meta">${avatar(t)}<b data-go="#profile/${t.user_id}">${esc(t.display_name)}</b><span class="muted">${fmt(t.created_at)}</span></div><div class="msg-body">${esc(t.body)}</div></div><div id="replies">${d.replies.map(replyHtml).join('')}</div></div>${composer('thread:'+id,'Reply to thread')}`);wire();wireComposer('thread:'+id)}
function replyHtml(r){return`<div class="post card" data-reply="${r.id}"><div class="post-meta">${avatar(r)}<b data-go="#profile/${r.user_id}">${esc(r.display_name)}</b><span class="muted">${fmt(r.created_at)}</span></div><div class="msg-body">${esc(r.body)}</div></div>`}
function appendReply(r){let box=$('#replies');if(box){box.insertAdjacentHTML('beforeend',replyHtml(r));wire();box.lastElementChild.scrollIntoView({block:'nearest'})}}
async function renderDMs(){S.active={kind:'dm'};app.innerHTML=shell(`<div class="topbar"><h2>Direct Messages</h2><button class="btn" data-action="newDM">New group</button></div><div class="content"><div class="card">${S.convs.map(c=>`<a class="row" data-go="#dm/${c.id}"><span class="badge">${c.unread||0}</span><div class="grow"><b>${esc(convName(c))}</b><small class="muted">${esc(c.last_body||'No messages yet')}</small></div></a>`).join('')||'<div class="empty">No conversations yet.</div>'}</div></div>`);wire()}
async function renderDM(id){S.active={kind:'dm',id};subscribe('direct:'+id);let info=await api('GET','/conversation/'+id);let side=`<aside class="side"><div class="side-head"><h1>${esc(info.conversation.name||'Conversation')}</h1><div class="nav-actions"><button class="btn secondary" id="addPeople">Add</button><button class="btn secondary" id="callBtn">Call</button></div></div><div class="list">${info.members.map(m=>`<a class="row" data-go="#profile/${m.user.id}">${avatar(m.user)}<div class="grow"><b>${esc(m.user.display_name)}</b><small class="muted">@${esc(m.user.username)}</small></div></a>`).join('')}</div></aside>`;app.innerHTML=shell(`<div class="topbar"><h2>${esc(info.conversation.name||'Direct Message')}</h2><button class="btn secondary" id="editConv">Edit</button></div><div class="messages" id="messages"></div>${composer('direct:'+id,'Message conversation')}`,side);wire();$('#addPeople').onclick=()=>addPeopleModal(id);$('#editConv').onclick=()=>editConversationModal(info.conversation);$('#callBtn').onclick=()=>joinCall(id);await loadMessages('direct',id);wireComposer('direct:'+id)}
async function renderServer(id){S.active={kind:'server',id};subscribe('server:'+id);let d=await api('GET','/server/'+id);let s=d.server;let side=`<aside class="side"><div class="side-head"><h1>${esc(s.name)}</h1><small>${esc(s.description)}</small><div class="nav-actions"><button class="btn secondary" id="inviteBtn">Invite</button><button class="btn secondary" id="newChannel">Channel</button></div></div><div class="list">${d.channels.map(c=>`<a class="row" data-go="${c.kind==='voice'?'#voice/'+c.id:'#channel/'+c.id}"><span class="server-icon">${c.kind==='voice'?'♪':'#'}</span><div class="grow"><b>${esc(c.name)}</b><small class="muted">${esc(c.kind)}</small></div></a>`).join('')}</div></aside>`;let right=`<aside class="right"><h3>Members</h3><div class="list">${d.members.map(m=>`<a class="row" data-go="#profile/${m.user.id}">${avatar(m.user)}<div class="grow"><b>${esc(m.user.display_name)}</b><small class="muted">${esc(m.role)} · ${esc(m.user.status||'')}</small></div></a>`).join('')}</div><div id="voiceBox" class="voice"></div></aside>`;app.innerHTML=shell(`<div class="topbar"><h2>${esc(s.name)}</h2></div><div class="content"><div class="card pad"><h2>${esc(s.name)}</h2><p class="muted">${esc(s.description)}</p><p>Select a text channel or voice channel.</p></div></div>`,side,right);wire();$('#inviteBtn').onclick=()=>inviteModal(id);$('#newChannel').onclick=()=>channelModal(id)}
async function renderChannel(id){S.active={kind:'channel',id};subscribe('channel:'+id);let d=await findServerForChannel(id);let ch=d.channels.find(c=>c.id===id);let side=`<aside class="side"><div class="side-head"><h1>${esc(d.server.name)}</h1><button class="btn secondary" data-go="#server/${d.server.id}">Back</button></div><div class="list">${d.channels.map(c=>`<a class="row ${c.id===id?'active':''}" data-go="${c.kind==='voice'?'#voice/'+c.id:'#channel/'+c.id}"><span class="server-icon">${c.kind==='voice'?'♪':'#'}</span><div class="grow"><b>${esc(c.name)}</b></div></a>`).join('')}</div></aside>`;app.innerHTML=shell(`<div class="topbar"><h2># ${esc(ch?.name||'channel')}</h2></div><div class="messages" id="messages"></div>${composer('channel:'+id,'Message #'+(ch?.name||''))}`,side);wire();await loadMessages('channel',id);wireComposer('channel:'+id)}

async function renderVoiceChannel(id){let d=await findServerForChannel(id);let ch=d.channels.find(c=>c.id===id);let side=`<aside class="side"><div class="side-head"><h1>${esc(d.server.name)}</h1><button class="btn secondary" data-go="#server/${d.server.id}">Back</button></div><div class="list">${d.channels.map(c=>`<a class="row ${c.id===id?'active':''}" data-go="${c.kind==='voice'?'#voice/'+c.id:'#channel/'+c.id}"><span class="server-icon">${c.kind==='voice'?'♪':'#'}</span><div class="grow"><b>${esc(c.name)}</b></div></a>`).join('')}</div></aside>`;S.active={kind:'voice',id};app.innerHTML=shell(`<div class="topbar"><h2>♪ ${esc(ch?.name||'voice')}</h2><button class="btn" id="joinV">Join voice</button></div><div class="content"><div class="card pad"><h2>Voice channel</h2><p class="muted">Peer-to-peer audio uses WebRTC signaling through the Erlang WebSocket hub.</p></div></div>`,side);wire();$('#joinV').onclick=()=>joinVoice(id)}

async function findServerForChannel(cid){for(let s of S.servers){try{let d=await api('GET','/server/'+s.id);if(d.channels.some(c=>c.id===cid))return d}catch{}}throw Error('channel not found')}
async function renderFriends(){S.active={kind:'friends'};app.innerHTML=shell(`<div class="topbar"><h2>Friends</h2><button class="btn" id="findUser">Find user</button></div><div class="content"><div class="card">${S.friends.map(f=>`<div class="row"><div data-go="#profile/${f.user.id}">${avatar(f.user)}</div><div class="grow"><b>${esc(f.user.display_name)}</b><small class="muted">@${esc(f.user.username)} · ${esc(f.status)}${f.incoming?' · incoming':''}</small></div>${f.incoming?`<button class="btn" data-accept="${f.user.id}">Accept</button>`:''}<button class="btn secondary" data-dm="${f.user.id}">DM</button></div>`).join('')||'<div class="empty">No friends yet.</div>'}</div></div>`);wire();$('#findUser').onclick=()=>searchUsersModal();$$('[data-accept]').forEach(b=>b.onclick=async()=>{await api('POST','/friends/accept',{user_id:+b.dataset.accept});await silentSync(true);renderFriends()});$$('[data-dm]').forEach(b=>b.onclick=async()=>{let r=await api('POST','/conversations',{user_ids:[+b.dataset.dm],name:''});go('#dm/'+r.id)})}
async function renderProfile(id){let p=await api('GET','/profile/'+id);let u=p.user;app.innerHTML=shell(`<div class="topbar"><h2>Profile</h2></div><div class="content"><div class="card profile"><div class="banner" style="${u.banner_url?`background-image:url('${esc(u.banner_url)}');background-size:cover`:''}"></div><div class="profile-body">${avatar(u,'big')}<div><h1>${esc(u.display_name)}</h1><p class="muted">@${esc(u.username)} · seen ${ago(u.last_seen)} ago</p><p>${esc(u.bio||'No bio set.')}</p><p><span class="pill">${esc(u.status||'')}</span> <span class="pill">${esc(p.relationship.status||'none')}</span></p><button class="btn" id="addFriend">Friend</button> <button class="btn secondary" id="dmUser">Message</button></div></div></div></div>`);wire();$('#addFriend').onclick=async()=>{await api('POST','/friends/request',{user_id:id});toast('request sent')};$('#dmUser').onclick=async()=>{let r=await api('POST','/conversations',{user_ids:[id],name:''});go('#dm/'+r.id)}}
async function renderSettings(){let u=S.me;app.innerHTML=shell(`<div class="topbar"><h2>Settings</h2><button class="btn danger" id="logout">Logout</button></div><div class="content"><div class="card pad"><h2>Profile</h2><div class="field"><label>Display name</label><input id="display" value="${esc(u.display_name)}"></div><div class="field"><label>Status</label><input id="status" value="${esc(u.status||'')}"></div><div class="field"><label>Bio</label><textarea id="bio" rows="5">${esc(u.bio||'')}</textarea></div><div class="field"><label>Avatar URL or small data image</label><input id="avatar" value="${esc(u.avatar_url||'')}"><input type="file" id="avatarFile" accept="image/*"></div><div class="field"><label>Banner URL or small data image</label><input id="banner" value="${esc(u.banner_url||'')}"><input type="file" id="bannerFile" accept="image/*"></div><button class="btn" id="saveProfile">Save</button></div></div>`);wire();$('#logout').onclick=async()=>{await api('POST','/logout',{});location.reload()};$('#avatarFile').onchange=e=>readImage(e.target.files[0],$('#avatar'));$('#bannerFile').onchange=e=>readImage(e.target.files[0],$('#banner'));$('#saveProfile').onclick=async()=>{await api('POST','/profile',{display_name:$('#display').value,status:$('#status').value,bio:$('#bio').value,avatar_url:$('#avatar').value,banner_url:$('#banner').value});await silentSync(true);renderSettings()}}
function readImage(file,input){if(!file||file.size>260000)return toast('image too large; use under 260 KB');let r=new FileReader();r.onload=()=>input.value=r.result;r.readAsDataURL(file)}
async function renderNewServer(){S.active={kind:'server'};app.innerHTML=shell(`<div class="topbar"><h2>Create server</h2></div><div class="content"><div class="card pad"><div class="field"><label>Name</label><input id="sName"></div><div class="field"><label>Description</label><textarea id="sDesc"></textarea></div><button class="btn" id="createServer">Create</button></div></div>`);wire();$('#createServer').onclick=async()=>{let r=await api('POST','/servers',{name:$('#sName').value,description:$('#sDesc').value});await silentSync(true);go('#server/'+r.id)}}
async function renderNotifications(){S.active={kind:'home'};app.innerHTML=shell(`<div class="topbar"><h2>Notifications</h2><button class="btn secondary" id="seen">Mark seen</button></div><div class="content"><div class="card">${S.notifs.map(n=>`<a class="row notif ${n.seen?'':'unseen'}" data-go="${esc(n.url)}"><div class="grow"><b>${esc(n.kind)}</b><small>${esc(n.body)}</small></div><span class="muted">${ago(n.created_at)} ago</span></a>`).join('')||'<div class="empty">No notifications.</div>'}</div></div>`);wire();$('#seen').onclick=async()=>{await api('POST','/notifications/seen',{});await silentSync(true);renderNotifications()}}
async function renderSearch(q){let users=await api('GET','/users?q='+encodeURIComponent(q));let threads=await api('GET','/threads?q='+encodeURIComponent(q));app.innerHTML=shell(`<div class="topbar"><h2>Search: ${esc(q)}</h2></div><div class="content"><h3>Users</h3><div class="card">${users.map(u=>`<a class="row" data-go="#profile/${u.id}">${avatar(u)}<div><b>${esc(u.display_name)}</b><small class="muted">@${esc(u.username)}</small></div></a>`).join('')||'<div class="empty">No users.</div>'}</div><h3>Threads</h3><div class="card">${threads.map(t=>`<a class="row" data-go="#thread/${t.id}"><div><b>${esc(t.title)}</b><small class="muted">${esc(t.forum_name)}</small></div></a>`).join('')||'<div class="empty">No threads.</div>'}</div></div>`);wire()}
async function loadMessages(scope,id,after){let q=`/messages?scope=${scope}&scope_id=${id}`+(after?'&after='+after:'');let msgs=await api('GET',q);if(!after){S.msg=msgs.reverse();renderMessages()}else msgs.forEach(appendMessage)}
async function updateActiveMessages(){let a=S.active;if(!$('#messages'))return;let last=S.msg[S.msg.length-1]?.id;if(a.kind==='channel'&&last)await loadMessages('channel',a.id,last);if(a.kind==='dm'&&last)await loadMessages('direct',a.id,last)}
function renderMessages(){let box=$('#messages');if(!box)return;box.innerHTML=S.msg.map((m,i)=>msgHtml(m,S.msg[i-1])).join('')||'<div class="empty">No messages yet.</div>';box.scrollTop=box.scrollHeight;wire()}
function msgHtml(m,prev){let same=prev&&prev.user_id===m.user_id&&(m.created_at-prev.created_at)<300000;return `<div class="msg ${same?'compact':''}" data-mid="${m.id}">${avatar(m)}<div>${same?'':`<div class="msg-head"><span class="msg-name" data-go="#profile/${m.user_id}">${esc(m.display_name)}</span><span class="msg-time">${fmt(m.created_at)}</span></div>`}<div class="msg-body">${esc(m.body)}</div></div></div>`}
function appendMessage(m){if(S.msg.some(x=>x.id===m.id))return;let box=$('#messages');if(!box)return;let near=box.scrollHeight-box.scrollTop-box.clientHeight<160;if(box.querySelector('.empty'))box.innerHTML='';let prev=S.msg[S.msg.length-1];S.msg.push(m);box.insertAdjacentHTML('beforeend',msgHtml(m,prev));wire();if(near)box.scrollTop=box.scrollHeight}
function composer(key,ph){return `<div class="composer"><textarea id="compose" placeholder="${esc(ph)}">${esc(S.drafts[key]||'')}</textarea><button class="btn" id="sendBtn">Send</button></div>`}
function wireComposer(key){let ta=$('#compose'),btn=$('#sendBtn');if(!ta||!btn)return;ta.oninput=()=>{S.drafts[key]=ta.value;saveDrafts()};ta.onkeydown=e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();btn.click()}};btn.onclick=async()=>{let body=ta.value.trim();if(!body)return;ta.value='';S.drafts[key]='';saveDrafts();try{if(key.startsWith('channel:'))appendMessage(await api('POST','/channels/'+key.split(':')[1]+'/messages',{body}));else if(key.startsWith('direct:'))appendMessage(await api('POST','/conversation/'+key.split(':')[1]+'/messages',{body}));else appendReply(await api('POST','/thread/'+key.split(':')[1]+'/replies',{body}))}catch(e){toast(e.message)}}}
function modal(body){closeModal();let d=document.createElement('div');d.className='modal';d.innerHTML=`<div class="modal-card"><div class="modal-head"><b>Plainwire</b><button class="btn ghost" id="xModal">Close</button></div><div class="pad">${body}</div></div>`;document.body.appendChild(d);$('#xModal').onclick=closeModal;d.onclick=e=>{if(e.target===d)closeModal()}}
function closeModal(){$('.modal')?.remove()}
function searchUsersModal(){modal(`<h2>Find user</h2><input id="userQ" placeholder="username or display name"><div id="userResults"></div>`);$('#userQ').oninput=async e=>{let q=e.target.value.trim();if(q.length<2)return;let users=await api('GET','/users?q='+encodeURIComponent(q));$('#userResults').innerHTML=users.map(u=>`<div class="row"><div data-go="#profile/${u.id}">${avatar(u)}</div><div class="grow"><b>${esc(u.display_name)}</b><small class="muted">@${esc(u.username)}</small></div><button class="btn secondary" data-friend="${u.id}">Add</button></div>`).join('');wire();$$('[data-friend]').forEach(b=>b.onclick=async()=>{await api('POST','/friends/request',{user_id:+b.dataset.friend});toast('request sent')})}}
function newDMModal(){modal(`<h2>New direct/group message</h2><p class="muted">Enter usernames separated by commas.</p><input id="dmUsers"><div class="field"><label>Group name optional</label><input id="dmName"></div><button class="btn" id="makeDM">Create</button>`);$('#makeDM').onclick=async()=>{let names=$('#dmUsers').value.split(',').map(x=>x.trim()).filter(Boolean);let ids=[];for(let n of names){let u=await api('GET','/users?q='+encodeURIComponent(n));if(u[0])ids.push(u[0].id)}let r=await api('POST','/conversations',{name:$('#dmName').value,user_ids:ids});closeModal();await silentSync(true);go('#dm/'+r.id)}}
function addPeopleModal(id){modal(`<h2>Add people</h2><p class="muted">Usernames separated by commas.</p><input id="addUsers"><button class="btn" id="addBtn">Add</button>`);$('#addBtn').onclick=async()=>{let ids=[];for(let n of $('#addUsers').value.split(',').map(x=>x.trim()).filter(Boolean)){let u=await api('GET','/users?q='+encodeURIComponent(n));if(u[0])ids.push(u[0].id)}await api('POST','/conversation/'+id+'/members',{user_ids:ids});closeModal();renderDM(id)}}
function editConversationModal(c){modal(`<h2>Edit conversation</h2><input id="cName" value="${esc(c.name||'')}"><div class="field"><label>Avatar URL</label><input id="cAvatar" value="${esc(c.avatar_url||'')}"></div><button class="btn" id="saveC">Save</button>`);$('#saveC').onclick=async()=>{await api('POST','/conversation/'+c.id,{name:$('#cName').value,avatar_url:$('#cAvatar').value});closeModal();renderDM(c.id)}}
function inviteModal(sid){modal(`<h2>Create server invite</h2><p class="muted">0 uses means unlimited.</p><input id="maxUses" value="0"><button class="btn" id="makeInvite">Create invite</button><div id="inviteOut"></div>`);$('#makeInvite').onclick=async()=>{let r=await api('POST','/server/'+sid+'/invites',{max_uses:+$('#maxUses').value||0});let url=location.origin+'/'+r.url;$('#inviteOut').innerHTML=`<div class="field"><label>Invite link</label><input value="${esc(url)}" readonly></div>`;navigator.clipboard?.writeText(url);toast('invite copied')}}
function channelModal(sid){modal(`<h2>New channel</h2><input id="chName" placeholder="name"><div class="field"><select id="chKind"><option value="text">Text</option><option value="voice">Voice</option></select></div><button class="btn" id="makeCh">Create</button>`);$('#makeCh').onclick=async()=>{await api('POST','/server/'+sid+'/channels',{name:$('#chName').value,kind:$('#chKind').value});closeModal();renderServer(sid)}}
async function joinInvite(){let code=prompt('Invite code or link');if(!code)return;code=code.split('/invite/').pop().replace('#','');await acceptInvite(code)}
async function acceptInvite(code){try{let r=await api('POST','/invites/'+encodeURIComponent(code)+'/join',{});await silentSync(true);go('#server/'+r.server_id)}catch(e){toast(e.message)}}
async function joinVoice(channelId){await leaveVoice();try{S.voice.stream=await navigator.mediaDevices.getUserMedia({audio:true,video:false});S.voice.mode='voice';S.voice.id=channelId;sendWS({type:'voice_join',channel_id:channelId});renderVoiceBox()}catch(e){toast('microphone unavailable')}}
async function joinCall(cid){await leaveVoice();try{S.voice.stream=await navigator.mediaDevices.getUserMedia({audio:true,video:false});S.voice.mode='call';S.voice.id=cid;sendWS({type:'call_join',conversation_id:cid});renderVoiceBox()}catch(e){toast('microphone unavailable')}}
async function leaveVoice(){if(S.voice.mode==='voice')sendWS({type:'voice_leave'});if(S.voice.mode==='call')sendWS({type:'call_leave'});for(let id of [...S.voice.peers.keys()])closePeer(id);S.voice.stream?.getTracks().forEach(t=>t.stop());S.voice={mode:null,id:null,stream:null,peers:new Map(),users:new Map(),muted:false,deafened:false};renderVoiceBox()}
async function ensurePeer(uid,offer){if(S.voice.peers.has(uid))return S.voice.peers.get(uid);let pc=new RTCPeerConnection({iceServers:[{urls:'stun:stun.l.google.com:19302'}]});S.voice.stream?.getTracks().forEach(t=>pc.addTrack(t,S.voice.stream));pc.onicecandidate=e=>{if(e.candidate)signal(uid,{kind:'ice',candidate:e.candidate})};pc.ontrack=e=>{let a=$('#audio_'+uid)||document.body.appendChild(Object.assign(document.createElement('audio'),{id:'audio_'+uid,autoplay:true}));a.srcObject=e.streams[0];a.muted=S.voice.deafened};S.voice.peers.set(uid,pc);if(offer){let o=await pc.createOffer();await pc.setLocalDescription(o);signal(uid,{kind:'offer',sdp:o})}return pc}
function signal(to,signal){if(S.voice.mode==='voice')sendWS({type:'voice_signal',to_user_id:to,signal});else sendWS({type:'call_signal',to_user_id:to,signal})}
async function handleSignal(from,sig){let pc=await ensurePeer(from,false);if(sig.kind==='offer'){await pc.setRemoteDescription(sig.sdp);let a=await pc.createAnswer();await pc.setLocalDescription(a);signal(from,{kind:'answer',sdp:a})}else if(sig.kind==='answer')await pc.setRemoteDescription(sig.sdp);else if(sig.kind==='ice')try{await pc.addIceCandidate(sig.candidate)}catch{}}
function closePeer(id){S.voice.peers.get(id)?.close();S.voice.peers.delete(id);$('#audio_'+id)?.remove()}
function voiceEvent(e){if(e.type==='voice_state'){S.voice.users=new Map((e.users||[]).map(u=>[u.user_id,u]));renderVoiceBox()}if(e.type==='voice_peer_joined'){S.voice.users.set(e.user_id,{user_id:e.user_id,profile:e.profile});renderVoiceBox();ensurePeer(e.user_id,true)}if(e.type==='voice_peer_left'){S.voice.users.delete(e.user_id);closePeer(e.user_id);renderVoiceBox()}if(e.type==='voice_signal')handleSignal(e.from_user_id,e.signal)}
function callEvent(e){if(e.type==='call_state'){S.voice.users=new Map((e.users||[]).map(u=>[u.user_id,u]));renderVoiceBox()}if(e.type==='call_peer_joined'){S.voice.users.set(e.user_id,{user_id:e.user_id,profile:e.profile});renderVoiceBox();ensurePeer(e.user_id,true)}if(e.type==='call_peer_left'){S.voice.users.delete(e.user_id);closePeer(e.user_id);renderVoiceBox()}if(e.type==='call_signal')handleSignal(e.from_user_id,e.signal)}
function renderVoiceBox(){let box=$('#voiceBox');if(!box)return;box.innerHTML=S.voice.mode?`<b>${S.voice.mode==='call'?'Call':'Voice'} connected</b><br><small>${S.voice.id}</small>${[...S.voice.users.values()].map(u=>`<div class="voice-user">${avatar(u.profile||{})}<span>${esc(u.profile?.display_name||('User '+u.user_id))}</span>${u.muted?'🔇':''}${u.deafened?'🧏':''}</div>`).join('')}<button class="btn secondary" id="muteBtn">${S.voice.muted?'Unmute':'Mute'}</button> <button class="btn secondary" id="deafenBtn">${S.voice.deafened?'Undeafen':'Deafen'}</button> <button class="btn danger" id="leaveV">Leave</button>`:`<b>Voice</b><br><small>Join a voice channel or start a DM call.</small>`;$('#muteBtn')?.addEventListener('click',()=>{S.voice.muted=!S.voice.muted;S.voice.stream?.getAudioTracks().forEach(t=>t.enabled=!S.voice.muted);sendWS({type:S.voice.mode==='call'?'call_state':'voice_state',patch:{muted:S.voice.muted,deafened:S.voice.deafened}});renderVoiceBox()});$('#deafenBtn')?.addEventListener('click',()=>{S.voice.deafened=!S.voice.deafened;$$('audio[id^="audio_"]').forEach(a=>a.muted=S.voice.deafened);sendWS({type:S.voice.mode==='call'?'call_state':'voice_state',patch:{muted:S.voice.muted,deafened:S.voice.deafened}});renderVoiceBox()});$('#leaveV')?.addEventListener('click',leaveVoice)}
window.addEventListener('hashchange',render);boot();
})();
